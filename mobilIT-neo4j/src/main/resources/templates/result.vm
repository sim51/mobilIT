{ "type": "FeatureCollection",
  "features": [
    #foreach( $relation in $path )
        { "type": "Feature",
          "geometry": {
            "type": "LineString",
            "coordinates": [
              #foreach( $seg in $relation.line )
                [$seg.latitude, $seg.longitude]#if( $foreach.hasNext ),#end
              #end
            ]
          },
          "properties": {
            "name": "$relation.name",
            "length": $relation.distance
          }
        },
    #end
  ]
}
